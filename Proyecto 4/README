Consideraciones importantes antes de ejecutar el programa

1) Instalar la biblioteca passlib y bycrpt
2) Los usuarios y contraseñas(Encriptadas) se guardan en usuarios.txt
3) Asegurate que no haya espacios en blanco en el txt
4) El servidor si lo estas viendo en VSC con solo darle play se ejecuta
5) El cliente hay que hacerle en una nueva consola un python Cliente.python
6) Si quieres agregar un nuevo usuario cuando entres en el cliente agrega cualquier cosa como usuario
despues te dira que el usuario no esta registrado y le agregas la contraseña que quieras, el programa 
automaticamente lo codificara, en el archivo de a.py puedes cambiarlo manualmente las contraseñas
despues se quita o se deja.
7) Una vez que se registre el usuario nuevo, el cliente se desconectara, hay que hacer la conexion denuevo.

